/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  env: {
    NEXT_PUBLIC_APP_NAME: 'WriteFlow AI',
    NEXT_PUBLIC_APP_DESCRIPTION: 'AI-powered writing tools for professionals',
  },
}

module.exports = nextConfig
